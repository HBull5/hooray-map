const step1 = document.querySelector( '#step-1 img' );
const map = document.querySelector( '.map' );

window.addEventListener( 'resize', () => {
    // step1.style.transform = `translateY( -${ window.innerHeight * 0.024 }px )`;
    // step1.style.top = `${ window.innerHeight * 0.03 }%`;
    // console.log( 'Window: ' + window.innerHeight );
    // console.log( 'Map: ' + map.offsetHeight );
});